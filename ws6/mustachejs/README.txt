
Modular pages with dynamic data using Mustache and nodes module system (require)
so this is ES5

Install:
  npm install -g nodemon (see https://www.npmjs.com/package/nodemon)
  npm install

Run:
  $ nodemon app.js (if nodemon installed)

  Visit localhost:8080
